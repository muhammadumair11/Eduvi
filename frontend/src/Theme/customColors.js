const colorObject = {
    primaryLight: "#0A033C",
    primaryMain: "#9C4DF4",
    primaryDark: "#BE88FB",

    secondaryDark: "#FFA79B",
    secondaryMain: "#FF6652",
    secondaryLight: "#FF4128",
};

const customPrimaryColors = {
    main: colorObject.primaryMain,
    dark: colorObject.primaryDark,
    light: colorObject.primaryLight,
};

const customSecondaryColors = {
    main: colorObject.secondaryMain,
    dark: colorObject.secondaryDark,
    light: colorObject.secondaryLight,
    white: "#FFFFFF",
};

const textColors = {
    primary: "#0A033C",
    secondary: "#FF6652",
    disabled: "#aaa",
};

const secondaryboxShadowColors = {
    light: "#ffa79b6b",
};

export {
    customPrimaryColors,
    customSecondaryColors,
    textColors,
    secondaryboxShadowColors,
};
